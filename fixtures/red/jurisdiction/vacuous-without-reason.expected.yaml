# Expectation for vacuous-without-reason.yaml
#
# Data, not code. The harness reads this file; it does not carry these values itself.
# A red fixture must fail AT THIS PATH with THIS KEYWORD. "Fails at all" is how a parse
# error impersonates a caught defect, so failing for any other reason fails the run.

fixture: vacuous-without-reason
finding: RF-14
expect:
  instancePath: "/invariants/INV-04"
  keyword: required
why: >-
  Vacuity must be argued, not asserted.
