# Expectation for inv03-character-licensed.yaml
#
# Data, not code. The harness reads this file; it does not carry these values.
# A red fixture MUST fail at this path with this keyword. Failing merely somewhere is
# how a parse error impersonates a caught defect, so any other failure fails the run.

fixture: inv03-character-licensed
finding: RF-09
expect:
  instancePath: "/strata/character"
  keyword: enum
why: >-
  strata.character = licensed. INV-03 hard constraint: strata 4-5 may never be licensed. No deferral, no grade, no bootstrap exemption, at any gate.
