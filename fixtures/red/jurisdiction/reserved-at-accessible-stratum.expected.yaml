# Expectation for reserved-at-accessible-stratum.yaml
#
# Data, not code. The harness reads this file; it does not carry these values.
# A red fixture MUST fail at this path with this keyword. Failing merely somewhere is
# how a parse error impersonates a caught defect, so any other failure fails the run.

fixture: reserved-at-accessible-stratum
finding: RF-12
expect:
  instancePath: "/does_not_decide/1/stratum"
  keyword: enum
why: >-
  disposition reserved citing stratum acts. Only character and destiny reserve; dressing an ordinary omission as a sacred limit must fail.
