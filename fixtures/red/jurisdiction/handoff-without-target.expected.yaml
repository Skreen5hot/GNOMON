# Expectation for handoff-without-target.yaml
#
# Data, not code. The harness reads this file; it does not carry these values.
# A red fixture MUST fail at this path with this keyword. Failing merely somewhere is
# how a parse error impersonates a caught defect, so any other failure fails the run.

fixture: handoff-without-target
finding: RF-11
expect:
  instancePath: "/does_not_decide/0"
  keyword: required
why: >-
  A declined question disposed as handoff with no to. The v0 defect: routing that goes nowhere a parser can follow, because it lived in a YAML comment.
