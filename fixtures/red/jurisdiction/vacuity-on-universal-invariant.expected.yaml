# Expectation for vacuity-on-universal-invariant.yaml
#
# Data, not code. The harness reads this file; it does not carry these values.
# A red fixture MUST fail at this path with this keyword. Failing merely somewhere is
# how a parse error impersonates a caught defect, so any other failure fails the run.

fixture: vacuity-on-universal-invariant
finding: RF-15
expect:
  instancePath: "/invariants/INV-01/status"
  keyword: const
why: >-
  INV-01 claimed vacuously-satisfied. INV-01 is stated universally - every essay locates its subject - so vacuity is not available to it.
