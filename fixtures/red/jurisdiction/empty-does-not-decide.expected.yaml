# Expectation for empty-does-not-decide.yaml
#
# Data, not code. The harness reads this file; it does not carry these values.
# A red fixture MUST fail at this path with this keyword. Failing merely somewhere is
# how a parse error impersonates a caught defect, so any other failure fails the run.

fixture: empty-does-not-decide
finding: RF-01
expect:
  instancePath: "/does_not_decide"
  keyword: minItems
why: >-
  does_not_decide is empty. THE load-bearing check: an essay that declines to declare its limits fails lint before it reaches review.
