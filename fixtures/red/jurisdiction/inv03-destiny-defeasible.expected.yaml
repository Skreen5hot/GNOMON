# Expectation for inv03-destiny-defeasible.yaml
#
# Data, not code. The harness reads this file; it does not carry these values.
# A red fixture MUST fail at this path with this keyword. Failing merely somewhere is
# how a parse error impersonates a caught defect, so any other failure fails the run.

fixture: inv03-destiny-defeasible
finding: RF-10
expect:
  instancePath: "/strata/destiny"
  keyword: enum
why: >-
  strata.destiny = defeasible. The same constraint from the softer side: a defeasible verdict on destiny is still a verdict on destiny.
