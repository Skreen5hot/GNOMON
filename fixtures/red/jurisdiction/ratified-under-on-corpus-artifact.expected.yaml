# Expectation for ratified-under-on-corpus-artifact.yaml
#
# Data, not code. The harness reads this file; it does not carry these values.
# A red fixture MUST fail at this path with this keyword. Failing merely somewhere is
# how a parse error impersonates a caught defect, so any other failure fails the run.

fixture: ratified-under-on-corpus-artifact
finding: RF-17
expect:
  instancePath: "/ratified-under"
  keyword: not
why: >-
  A corpus panel carrying ratified-under. The two marks have disjoint scopes by construction (CHARTER 4.3): corpus prose never self-ratifies, so the grant mark is illegal here.
