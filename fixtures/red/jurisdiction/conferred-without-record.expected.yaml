# Expectation for conferred-without-record.yaml
#
# Data, not code. The harness reads this file; it does not carry these values.
# A red fixture MUST fail at this path with this keyword. Failing merely somewhere is
# how a parse error impersonates a caught defect, so any other failure fails the run.

fixture: conferred-without-record
finding: RF-18
expect:
  instancePath: "/ratification"
  keyword: required
why: >-
  ratification conferred with no record. A conferral claim with nothing to point at is the bare-declared defect wearing a different field name.
